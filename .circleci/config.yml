# iOS CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/ios-migrating-from-1-2/ for more details
#
version: 2.1

commands:
  early_return_for_forked_prs:
    description: >-
      If this build is from a fork, stop executing the current job and return success.
      This is useful to avoid steps that will fail due to missing credentials.
    steps:
      - run:
          name: skip left steps and return immediately from current job if current build is from a forked PR
          command: |
            if [ -n "$CIRCLE_PR_NUMBER" ]; then
              echo "Nothing to do for forked PRs, so marking this step successful"
              circleci step halt
            fi

  update_brew:
    description: >-
      Update Homebrew
    steps:
      - run:
          name: Update Homebrew
          command: |
            HOMEBREW_LOGS=~/homebrew-logs
            HOMEBREW_TEMP=~/homebrew-temp
            brew update

  set_environment_variables:
    description: >-
      Set environment variables
    steps:
      - run:
          name: Set environment variables
          command: |
            sdkName=aws-ios-sdk
            echo "export sdkName=$sdkName" >> $BASH_ENV

            release_bucket=${S3_RELEASE_BUCKET}
            echo "export release_bucket=$release_bucket" >> $BASH_ENV

            release_tag=${CIRCLE_TAG}
            echo "export release_tag=$release_tag" >> $BASH_ENV

            release_version=${CIRCLE_TAG}
            echo "export release_version=$release_version" >> $BASH_ENV

            sdkNameWithVersion="$sdkName-$release_version"
            echo "export sdkNameWithVersion=$sdkNameWithVersion" >> $BASH_ENV

            pre_name=$(echo "${CIRCLE_SHA1}" | cut -c1-7)
            override_test_variable_name=OVERRIDE_TEST_${pre_name}
            echo "export override_test_variable_name=$override_test_variable_name" >> $BASH_ENV

  quit_for_nominorversion:
    description: >-
      If current release is not a minor version bump, stop executing the current job and return success.
    steps:
      - run:
          name: Skip remaining steps if current release is not a minor version bump
          command: |
            echo $release_version
            maintenance=$(echo $release_version | sed "s/[0-9]*.[0-9]*.\([0-9]*\)/\1/")
            echo $maintenance
            if [ "$maintenance" != "0" ]; then
              echo "This is not minor version bump release, so marking this step successful"
              circleci step halt
            fi

  bump_version_pre:
    description: >-
      Prepare bump version
    steps:
      - run:
          name: Checkout repository for bump version
          command: |
            if [ -z "$target_branch" ]
            then
              target_branch="main"
            fi

            git config --local user.name "${GITHUB_BUMPVERSION_USER}"
            git clone "https://github.com/${GITHUB_BUMPVERSION_USER}/${bumpversion_repo_name}.git"
            cd ${bumpversion_repo_name}
            git fetch
            branches=$(git branch)
            if [[ $branches == *"bump_version"* ]]; then
              echo "the branch is already pesent"
              git checkout bump_version
            else
              echo "create new branch bump_version"
              git checkout -b bump_version
            fi
            git remote add upstream https://github.com/${bumpversion_repo_user}/${bumpversion_repo_name}
            git remote -v
            git fetch upstream
            git reset --hard upstream/$target_branch
            echo "push update the branch"
            git push --force -q https://${GITHUB_BUMPVERSION_TOKEN}@github.com/${GITHUB_BUMPVERSION_USER}/${bumpversion_repo_name}.git

  bump_version_post:
    description: >-
      Check in bump version change
    steps:
      - run:
          name: Stage changes
          command: |
            cd ${bumpversion_repo_name}
            git config --local user.name "${GITHUB_BUMPVERSION_USER}"
            gitstatus=$(git status)
            echo $gitstatus
            if [[ $gitstatus == *"Changes not staged for commit:"* ]]; then
              git add .
            else
              echo "No changes for sample application bump version"
              circleci step halt
            fi
      - run:
          name: Check in changes
          command: |
            if [ -z "$target_branch" ]
            then
              target_branch="main"
            fi
            cd ${bumpversion_repo_name}
            git status
            git config --local user.name "${GITHUB_BUMPVERSION_USER}"
            git commit -m "${bump_version_message}"
            git push --force -q https://${GITHUB_BUMPVERSION_TOKEN}@github.com/${GITHUB_BUMPVERSION_USER}/${bumpversion_repo_name}.git
            title="${bump_version_pr_title}"
            content="${bump_version_message}"
            echo "title:$title"
            echo "content:$content"
            python3 ../CircleciScripts/create_pullrequest.py "${GITHUB_BUMPVERSION_USER}" "${GITHUB_BUMPVERSION_TOKEN}" "$title" "$content" "$target_branch" "${GITHUB_BUMPVERSION_USER}:bump_version" ${bumpversion_repo_user} ${bumpversion_repo_name}

  configure_aws:
    description: >-
      Install aws cli and configure android aws release profile
    steps:
      - run:
          name: Install AWS cli
          command: |
            HOMEBREW_NO_AUTO_UPDATE=1 brew install awscli
      - run:
          name: Configure AWS profiles
          command: |
            aws configure --profile sdk_s3_release set region us-east-1
            echo -e "[sdk_s3_release]\naws_access_key_id=${S3_RELEASE_AWS_ACCESS_KEY_ID}\naws_secret_access_key=${S3_RELEASE_AWS_SECRET_ACCESS_KEY}\n" >> ~/.aws/credentials

  pre_start_simulator:
    description: >-
      Pre-start iOS Simulator, as the build may fail if it takes too long to start up during that step
    steps:
      - run:
          name: Pre-start iOS Simulator
          command: |
            bash CircleciScripts/pre_start_simulator.sh

  skip_test_job:
    description: >-
      Check if the test job can be skipped
    steps:
      - run:
          name: Skip the test job if commit message require skipping tests
          command: |
            commitmessage=$(git log --format=%B -n 1 ${CIRCLE_SHA1})
            if [[ "$commitmessage" =~ .*"[skip test]".* ]]
            then
               echo "skip the test job required from commit message"
               circleci step halt
            fi
      - run:
          name: Skip the test job if the release process was triggered by CircleCI
          command: |
            skipflag=$(echo "${SKIPTEST_FOR_RELEASE}") | tr '[:upper:]' '[:lower:]'
            if ! [ -z "${CIRCLE_TAG}" ] && [ "$skipflag" == "true" ]
            then
              tagmessage="git show ${CIRCLE_TAG}"
              echo $tagmessage
              if [[ "$tagmessage" =~ .*"Trigger release from circleci".* ]]
              then
                 echo "The release was triggered by CircleCI. Skip the job"
                 circleci step halt
              fi
            fi
      - run:
          name: Skip the test job if this was merged from develop by CircleCI
          command: |
            skipflag=$(echo "${SKIPTEST_FOR_MAIN}") | tr '[:upper:]' '[:lower:]'
            commitmessage=$(git log --format=%B -n 1 ${CIRCLE_SHA1})
            if [ "$skipflag" == "true" ] && [[ "$commitmessage" =~ .*"[Merge develop to main by circleci]".* ]]
            then
               echo "This is merged from develop by CircleCI. Skip the test job"
               circleci step halt
            fi

  override_test_job:
    description: >-
      Skip the test job if previously tested, or if the environment variable OVERRIDE_TEST is TRUE
    steps:
      - run:
          name: Skip test if the commit is already tested
          command: |
            echo "${TESTED_COMMITID}:${CIRCLE_SHA1}"
            skipflag=$(echo "${SKIPTESTEDCHANGE}" | tr '[:upper:]' '[:lower:]')
            if [ "${TESTED_COMMITID}" == "${CIRCLE_SHA1}" ] && [ "${skipflag}" != "true" ]
            then
              echo "Skipping test as the change ${CIRCLE_SHA1} has already been tested"
              circleci step halt
            fi
      - run:
          name: Skip test if OVERRIDE_TEST flag is set
          command: |
              echo "override_test_variable_name=$override_test_variable_name"
              override_test_value=$(eval "echo $"${override_test_variable_name}"")
              override_test_value=$(echo "${override_test_value}" | tr '[:upper:]' '[:lower:]')
              if [ "${override_test_value}" == "true" ]; then
                echo "project environment variable OVERRIDE_TEST is set with TRUE, skip all tests"
                circleci step halt
              fi

  skip_job_unless_required:
    description: >-
      Skip job unless required
    steps:
      - run:
          name: Skip job if SKIP_JOB flag is set
          command: |
              variable_name=SKIP_JOB_${CIRCLE_JOB}
              skipjob_flag=$(eval "echo $"${variable_name}"")
              skipjob_flag=$(echo "${skipjob_flag}" | tr '[:upper:]' '[:lower:]')
              if [ "${skipjob_flag}" == "true" ]
              then
                echo "Skipping current job as ${variable_name} environment variable is set"
                circleci step halt
              fi

jobs:
  build:
    macos:
      xcode: "11.6.0"
    steps:
      - skip_job_unless_required
      - checkout
      - set_environment_variables
      - pre_start_simulator
      - run:
          name: Build sdk
          command: |
            xcodebuild build -project AWSiOSSDKv2.xcodeproj -scheme AWSAllTests -sdk iphonesimulator -destination "${destination}"
            xcodebuild build -project AWSAuthSDK/AWSAuthSDK.xcodeproj -scheme AWSAuthSDKAllTargets -sdk iphonesimulator -destination "${destination}"

  unittest:
    macos:
      xcode: "11.6.0"
    steps:
      - set_environment_variables
      - override_test_job
      - skip_job_unless_required
      - checkout
      - skip_test_job
      - pre_start_simulator
      - run:
          name: Build SDK
          command: xcodebuild build-for-testing -project AWSiOSSDKv2.xcodeproj -scheme AWSAllTests -sdk iphonesimulator -destination "${destination}"
      - run :
          name: Run unit tests
          command: bash CircleciScripts/run_unittests.sh "test_result" "${destination}"
      - run:
          name : Check unit test result
          command : bash CircleciScripts/check_test_result.sh "test_result"
      - store_artifacts:
          path: test_result

  pre_integrationtest:
    macos:
      xcode: "11.6.0"
    environment:
      TERM: xterm-256color
    steps:
      - skip_job_unless_required
      - set_environment_variables
      - override_test_job
      - run:
          name: Get override test variable name
          command: |
            echo "override_test_variable_name=$override_test_variable_name"
      - run:
          name: Reset OVERRIDE_TEST environment variable
          command: |
            override_test_value=$(eval "echo $"${override_test_variable_name}"")
            if ! [ -z "$override_test_value" ]
            then
               echo "reset ${override_test_variable_name} value"
              parameters='{"name":"'${override_test_variable_name}'","value":"FALSE"}'
              curl -X POST \
                -H "Content-type: application/json" \
                -H "Accept: application/json" \
                -d "$parameters" \
                "https://circleci.com/api/v1.1/project/github/${CIRCLE_PROJECT_USERNAME}/${CIRCLE_PROJECT_REPONAME}/envvar?circle-token=${CIRCLE_API_USER_TOKEN}"
            fi
      - checkout
      - set_environment_variables
      - run:
          name: Generate testconfiguration.json
          command: bash Scripts/generate-test-config.sh -p ios -v
      - pre_start_simulator
      - run:
          name: Build SDK
          command: |
            xcodebuild build-for-testing -project AWSiOSSDKv2.xcodeproj -scheme AWSAllTests -sdk iphonesimulator -destination "${destination}" -derivedDataPath ./build_result/AWSiOSSDKv2
            xcodebuild build-for-testing -project AWSAuthSDK/AWSAuthSDK.xcodeproj -scheme AWSMobileClient -sdk iphonesimulator -destination "${destination}" -derivedDataPath ./build_result/AWSAuthSDK
      - save_cache:
          key: build_result-{{ .Revision }}
          paths:
            - build_result

  post_integrationtest:
    macos:
      xcode: "11.6.0"
    steps:
      #TODO need check why multiple key does not work here
      - skip_job_unless_required
      - override_test_job
      - restore_cache:
          key: test_result-{{ .Revision }}-AWSMobileClient
      - restore_cache:
          key: test_result-{{ .Revision }}-AutoScaling
      - restore_cache:
          key: test_result-{{ .Revision }}-CloudWatch
      - restore_cache:
          key: test_result-{{ .Revision }}-Cognito
      - restore_cache:
          key: test_result-{{ .Revision }}-Comprehend
      - restore_cache:
          key: test_result-{{ .Revision }}-Core
      - restore_cache:
          key: test_result-{{ .Revision }}-DynamoDB
      - restore_cache:
          key: test_result-{{ .Revision }}-EC2
      - restore_cache:
          key: test_result-{{ .Revision }}-ElasticLoadBalancing
      - restore_cache:
          key: test_result-{{ .Revision }}-IoT
      - restore_cache:
          key: test_result-{{ .Revision }}-KMS
      - restore_cache:
          key: test_result-{{ .Revision }}-Kinesis
      - restore_cache:
          key: test_result-{{ .Revision }}-KinesisVideoSignaling
      - restore_cache:
          key: test_result-{{ .Revision }}-Lambda
      - restore_cache:
          key: test_result-{{ .Revision }}-Lex
      - restore_cache:
          key: test_result-{{ .Revision }}-MobileAnalytics
      - restore_cache:
          key: test_result-{{ .Revision }}-Pinpoint
      - restore_cache:
          key: test_result-{{ .Revision }}-Polly
      - restore_cache:
          key: test_result-{{ .Revision }}-Rekognition
      - restore_cache:
          key: test_result-{{ .Revision }}-S3
      - restore_cache:
          key: test_result-{{ .Revision }}-SES
      - restore_cache:
          key: test_result-{{ .Revision }}-SNS
      - restore_cache:
          key: test_result-{{ .Revision }}-SQS
      - restore_cache:
          key: test_result-{{ .Revision }}-Textract
      - restore_cache:
          key: test_result-{{ .Revision }}-Transcribe
      - restore_cache:
          key: test_result-{{ .Revision }}-TranscribeStreaming
      - restore_cache:
          key: test_result-{{ .Revision }}-Translate
      - store_artifacts:
          path: integration_test_result
      - run:
          name: set environment variables
          command: |
            parameters='{"name":"TESTED_COMMITID","value":"'${CIRCLE_SHA1}'"}'
            curl -X POST \
              -H "Content-type: application/json" \
              -H "Accept: application/json" \
              -d "$parameters" \
              "https://circleci.com/api/v1.1/project/github/${CIRCLE_PROJECT_USERNAME}/${CIRCLE_PROJECT_REPONAME}/envvar?circle-token=${CIRCLE_API_USER_TOKEN}"

  integrationtest:
    description: Run integration tests
    parameters:
      groupname:
        default: stable_testList
        type: string
    macos:
      xcode: "11.6.0"
    steps:
      - set_environment_variables
      - override_test_job
      - skip_job_unless_required
      - early_return_for_forked_prs
      - checkout
      - skip_test_job
      - pre_start_simulator
      - restore_cache:
          key: build_result-{{ .Revision }}
      - run:
          name: Install JSON parser
          command: pip3 install demjson
      - run :
          name: Run integration tests
          command: |
            mkdir integration_test_result
            python3 CircleciScripts/run_integrationtests.py CircleciScripts/IntegrationTestsConfiguration.json "integration_test_result" "<< parameters.groupname >>" "${destination}" "./build_result"
      - run:
          name : Check integration test result
          command : |
            echo "testresult=$testresult"
            if [ "$testresult" == "0" ]
            then
                echo "test succeed!"
            else
                echo "There are test failures"
                exit 1
            fi
      - store_artifacts:
          path: integration_test_result
      - save_cache:
          key: test_result-{{ .Revision }}-{{ .Environment.CIRCLE_JOB }}
          paths:
            - integration_test_result

  release_tag:
    macos:
      xcode: "11.6.0"
    steps:
      - skip_job_unless_required
      - checkout
      - update_brew
      - run:
          name: Install github-release
          command: HOMEBREW_NO_AUTO_UPDATE=1 brew install github-release
      - run:
          name: Release the tag to GitHub
          command: |
            tagdescription=$(sed -n '/## '${CIRCLE_TAG}'/,/## [0-9]*\.[0-9]*\.[0-9]/p' CHANGELOG.md | sed '1d' | sed '$d')
            tagname="AWS SDK for iOS "${CIRCLE_TAG}
            echo "$tagdescription" | github-release release -s ${GITHUB_TOKEN} -u ${CIRCLE_PROJECT_USERNAME} -r ${CIRCLE_PROJECT_REPONAME} -t ${CIRCLE_TAG} --name "$tagname" -d -

  build_carthage:
    macos:
      xcode: "11.6.0"
    steps:
      - skip_job_unless_required
      - checkout
      - pre_start_simulator
      - update_brew
      - run:
          name: Upgrade Carthage
          command: |
            HOMEBREW_NO_AUTO_UPDATE=1 brew upgrade carthage
      - run:
          name: Build Carthage
          command: |
            # Build Carthage via this script to workaround lipo error due to multiple architectures in same file
            . ./CircleciScripts/carthage-build.sh --no-skip-current --platform iOS | tee buildout.txt
          no_output_timeout: 100m
      - save_cache:
          key: carthage_build_result-{{ .Revision }}
          paths:
            - Carthage

  release_carthage:
    macos:
      xcode: "11.6.0"
    steps:
      - skip_job_unless_required
      - checkout
      - pre_start_simulator
      - update_brew
      - run:
          name: Install github-release
          command: HOMEBREW_NO_AUTO_UPDATE=1 brew install github-release
      - run:
          name: Upgrade Carthage
          command: |
            HOMEBREW_NO_AUTO_UPDATE=1 brew upgrade carthage
      - restore_cache:
          key: carthage_build_result-{{ .Revision }}
      - run:
          name: Create Carthage archive
          command: python3 ./CircleciScripts/create_carthage_archive.py
          no_output_timeout: 20m
      - run:
          name: Upload Carthage archive to GitHub release
          command: github-release upload -s ${GITHUB_TOKEN} -u ${CIRCLE_PROJECT_USERNAME} -r ${CIRCLE_PROJECT_REPONAME} -t ${CIRCLE_TAG} -f aws-sdk-ios-carthage.framework.zip -n aws-sdk-ios-carthage.framework.zip

  lint_cocoapods_palpatim_test:
    macos:
      xcode: "11.6.0"
    steps:
      - skip_job_unless_required
      - checkout
      - run:
          # Note that this invocation of cocoapods_release requires at least
          # CocoaPods 1.9.3, and cocoalpods-trunk 1.5.0, since it uses the
          # `--synchronous` flag. CocoaPods 1.9.3 comes with the 11.6.0 Xcode
          # image, but just to be safe, we'll manually update cocoapods-trunk
          name: Update cocoapods-trunk to support --synchronous flag
          command: gem update cocoapods-trunk
      - run:
          name: Update local spec repo
          command: ./Scripts/setup_private_specs.sh 2.16.0 2.16.0
      - run:
          name: Lint all pods
          command: |
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSAPIGateway.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSAppleSignIn.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSAuth.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSAuthCore.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSAuthUI.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSAutoScaling.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSCloudWatch.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSCognito.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSCognitoAuth.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSCognitoIdentityProvider.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSCognitoIdentityProviderASF.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSCognitoSync.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSComprehend.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSConnect.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSConnectParticipant.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSCore.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSDynamoDB.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSEC2.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSElasticLoadBalancing.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSFacebookSignIn.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSGoogleSignIn.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSIoT.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSKMS.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSKinesis.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSKinesisVideo.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSKinesisVideoArchivedMedia.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSKinesisVideoSignaling.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSLambda.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSLex.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSLogs.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSMachineLearning.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSMobileAnalytics.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSMobileClient.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSPinpoint.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSPolly.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSRekognition.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSS3.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSSES.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSSNS.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSSQS.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSSageMakerRuntime.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSSimpleDB.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSTextract.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSTranscribe.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSTranscribeStreaming.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSTranslate.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSUserPoolsSignIn.podspec
            pod lib lint --sources=aws-sdk-ios-local-specs,trunk --allow-warnings AWSiOSSDKv2.podspec

  release_cocoapods:
    macos:
      xcode: "11.6.0"
    steps:
      - skip_job_unless_required
      - checkout
      - run:
          # Note that this invocation of cocoapods_release requires at least
          # CocoaPods 1.9.3, and cocoalpods-trunk 1.5.0, since it uses the
          # `--synchronous` flag. CocoaPods 1.9.3 comes with the 11.6.0 Xcode
          # image, but just to be safe, we'll manually update cocoapods-trunk
          name: Update cocoapods-trunk to support --synchronous flag
          command: gem update cocoapods-trunk
      - run:
          name: Release CocoaPods
          command: python3 ./CircleciScripts/cocoapods_release.py
          no_output_timeout: 20m

  release_docs:
    macos:
      xcode: "11.6.0"
    steps:
      - skip_job_unless_required
      - checkout
      - run:
          name: Install jazzy
          command: |
            sudo gem install jazzy --source http://rubygems.org
      - run:
          name: Generate documents
          command: bash ./CircleciScripts/generate_documentation.sh
      - run:
          name: Copy documents
          command: |
            git config --local user.name "AWS"
            git checkout gh-pages
            rm -rf docs/reference
            mv docs_tmp docs/reference
      - run:
          name: Checkin documents
          command: |
            for dirPath in docs/reference/* ; do
              sdkName=$( basename "$dirPath" )
              git add "$dirPath" && git commit -m "Update API documentation for ${sdkName} ${CIRCLE_TAG}"
              git prune
            done
            git push -q https://${GITHUB_TOKEN}@github.com/aws/aws-sdk-ios.git

  add_doc_tag:
    macos:
      xcode: "11.6.0"
    steps:
      - skip_job_unless_required
      - checkout
      - run:
          name: Add documentation tags to gh-pages
          command: |
            git config --local user.name "AWS"
            git checkout gh-pages
            git tag -a ${CIRCLE_TAG}_api_docs -m "Add documentation tags to version ${CIRCLE_TAG}"
            git push --tags -q https://${GITHUB_TOKEN}@github.com/aws-amplify/aws-sdk-ios.git

  bump_ios_sampleapp_version:
    macos:
      xcode: "11.6.0"
    steps:
      - skip_job_unless_required
      - set_environment_variables
      - quit_for_nominorversion
      - checkout
      - run:
          name: Checkout repository for bump version
          command: |
            SAMPLE_APPS_REPO_ACCOUNT=awslabs
            echo "export SAMPLE_APPS_REPO_ACCOUNT=$SAMPLE_APPS_REPO_ACCOUNT" >> $BASH_ENV
            git clone "https://github.com/${SAMPLE_APPS_REPO_ACCOUNT}/aws-sdk-ios-samples.git"
      - run:
          name: Update version
          command:
            python3 CircleciScripts/bump_iossample_version.py "$(pwd)/aws-sdk-ios-samples" $release_version
      - run:
          name: Build projects
          command:
            python3 CircleciScripts/build_iossample.py "$(pwd)/aws-sdk-ios-samples"
      - run:
          name: Stage changes
          command: |
            cd aws-sdk-ios-samples
            git config --local user.name "AWS"
            gitstatus=$(git status)
            echo $gitstatus
            if [[ $gitstatus == *"Changes not staged for commit:"* ]]; then
              git add .
            else
              echo "No changes for sample application bump version"
              circleci step halt
            fi
      - run:
          name: Check in changes
          command: |
            cd aws-sdk-ios-samples
            git status
            git config --local user.name "AWS"
            git commit -m "Update sample app dependency to ${release_version}"
            git push -q https://${GITHUB_BUMPVERSION_TOKEN}@github.com/${SAMPLE_APPS_REPO_ACCOUNT}/aws-sdk-ios-samples.git

  bump_ios_amplifydocs_version:
    macos:
      xcode: "11.6.0"
    steps:
      - skip_job_unless_required
      - set_environment_variables
      - quit_for_nominorversion
      - checkout
      - run:
          name: Set environment variables
          command: |
            bumpversion_repo_user=aws-amplify
            bumpversion_repo_name=docs
            minorversion=$(echo $release_version | sed "s/\([0-9]*.[0-9]*\).[0-9]*/\1/")
            echo ${minorversion}
            bump_version_message="Update iOS docs to ${release_version}"
            bump_version_pr_title="Update iOS docs to ${release_version}"
            echo "export bumpversion_repo_user=$bumpversion_repo_user" >> $BASH_ENV
            echo "export bumpversion_repo_name=$bumpversion_repo_name" >> $BASH_ENV
            echo "export minorversion=$minorversion" >> $BASH_ENV
            echo "export bump_version_message='${bump_version_message}'" >> $BASH_ENV
            echo "export bump_version_pr_title='${bump_version_pr_title}'" >> $BASH_ENV
            echo "bump_version_pr_title:$bump_version_pr_title"
      - bump_version_pre
      - run:
          name: Bump docs version
          command:
            python3 CircleciScripts/bump_iosdocs_version.py "$(pwd)/${bumpversion_repo_name}/ios" $release_version
      - bump_version_post

  release_ios_s3:
    macos:
      xcode: "11.6.0"
    environment:
      TERM: xterm-256color
    steps:
      - skip_job_unless_required
      - checkout
      - update_brew
      - set_environment_variables
      - configure_aws
      - restore_cache:
          key: carthage_build_result-{{ .Revision }}
      - run:
          name: Make directory structure
          command: |
            mkdir -p "$sdkNameWithVersion"
            mv "Carthage/Build/iOS" "$sdkNameWithVersion/frameworks"
            rm -rf "Carthage/Build"
      - run:
          name: Copy SDK resource files
          command: |
            python3 CircleciScripts/copy_resourcefiles.py "$(pwd)" "$(pwd)/$sdkNameWithVersion"
      - run:
          name: Zip SDK folder
          command: |
            zip -r "$sdkNameWithVersion.zip" "$sdkNameWithVersion"
      - run:
          name: Copy zip file
          command: |
            mkdir -p sdkfiles
            cp "$sdkNameWithVersion.zip" "sdkfiles/$sdkNameWithVersion.zip"
      - store_artifacts:
          path: sdkfiles
      - run:
          name: Upload to S3
          command: |
            aws s3api put-object --bucket "$release_bucket" --key "$sdkNameWithVersion.zip" --body "$sdkNameWithVersion.zip" --content-disposition "attachment;filename=$sdkNameWithVersion.zip" --acl public-read --profile sdk_s3_release
            aws s3api put-object --bucket "$release_bucket" --key "latest/$sdkName.zip" --body "$sdkNameWithVersion.zip" --content-disposition "attachment;filename=$sdkNameWithVersion.zip" --acl public-read --profile sdk_s3_release
      - run:
          name: Invalidate cloudfront
          command: |
            python3 CircleciScripts/cloudfront_invalidate.py sdk_s3_release "${S3_RELEASE_DISTRIBUTION_ID}" "latest/$sdkName.zip"

  bump_sdk_version:
    docker:
      - image: circleci/android:api-27-alpha
    environment:
      JVM_OPTS: -Xmx1024m
    steps:
      - skip_job_unless_required
      - set_environment_variables
      - checkout
      - run:
          name: Install python3-pip
          command: |
            echo "${newsdkversion}"
            sudo apt-get update
            sudo apt-get -y install python3-pip
      - run:
          name: Install JSON parser
          command: sudo pip3 install demjson
      - run:
          name: Install lxml
          command: sudo pip3 install lxml
      - run:
          name: Set environment variables
          command: |
            git config --local user.name AWS

            commitmessage=$(git log --format=%B -n 1 ${CIRCLE_SHA1})
            echo "commitmessage=$commitmessage"
            if [[ "$commitmessage" != *"bump version"* ]]
            then
               echo "To bump sdk version, the commit message should contain the words 'bump version'"
               circleci step halt
            fi

            release_version=$(head version | grep -E "^[0-9]+.[0-9]+.[0-9]+" | sed "s/^\([0-9]*.[0-9]*.[0-9]*\).*/\1/")
            if [ -z "release_version" ]
            then
              echo "version file does not have a correct version number"
              exit 1
            fi
            echo "release_version=[$release_version]"
            bumpversion_repo_user=${CIRCLE_PROJECT_USERNAME}
            bumpversion_repo_name=${CIRCLE_PROJECT_REPONAME}
            echo "bumpversion_repo_user:$bumpversion_repo_user"
            echo "bumpversion_repo_name:$bumpversion_repo_name"

            bump_version_message="[bump version $release_version]"
            bump_version_pr_title="bump iOS sdk version to ${release_version}"
            target_branch=develop
            echo "export bumpversion_repo_user=$bumpversion_repo_user" >> $BASH_ENV
            echo "export bumpversion_repo_name=$bumpversion_repo_name" >> $BASH_ENV
            echo "export release_version=$release_version" >> $BASH_ENV
            echo "export bump_version_message='${bump_version_message}'" >> $BASH_ENV
            echo "export bump_version_pr_title='${bump_version_pr_title}'" >> $BASH_ENV
            echo "export target_branch='${target_branch}'" >> $BASH_ENV
            echo "bump_version_pr_title:$bump_version_pr_title"
      - bump_version_pre
      - run:
          name: Bump SDK version
          command: |
            cp -R ${bumpversion_repo_name}/CircleciScripts/ CircleciScripts/
            python3 CircleciScripts/bump_sdk_version.py "$(pwd)/${bumpversion_repo_name}" $release_version
      - bump_version_post

  merge_to_main:
    docker:
      - image: circleci/android:api-27-alpha
    environment:
      JVM_OPTS: -Xmx1024m
    steps:
      - skip_job_unless_required
      - checkout
      - set_environment_variables
      - run:
          name: Merge change to main
          command: |
            git config --local user.name AWS
            git checkout main
            git fetch origin
            git reset origin/main
            git checkout .
            git clean -fd .
            commitlog=$(git log main -20)
            currentcommit="${CIRCLE_SHA1}"
            echo "currentcommit=$currentcommit"
            echo "$commitlog"
            if [[ "$commitlog" =~ .*"commit $currentcommit".* ]]
            then
               echo "The change is already merged"
            else
              echo "Merging change from develop to main"
              currentcommit="${CIRCLE_SHA1}"
              lastcommitmessage=$(git log --format=%B -n 1 ${currentcommit})

              git merge develop --no-edit --commit -m "[Merge develop to main by circleci] $lastcommitmessage"
              echo "push change"
              git push -q https://${GITHUB_TOKEN}@github.com/${CIRCLE_PROJECT_USERNAME}/${CIRCLE_PROJECT_REPONAME}.git
            fi

  prepare_release_sdk:
    docker:
      - image: circleci/android:api-27-alpha
    environment:
      JVM_OPTS: -Xmx1024m
    steps:
      - skip_job_unless_required
      - checkout
      - set_environment_variables
      - run:
          name: Trigger release_sdk if current commit is a bump version
          command: |
            bash CircleciScripts/prepare_release_sdk.sh

  create_pullrequest_for_modelupdate:
    docker:
      - image: circleci/android:api-27-alpha
    environment:
      JVM_OPTS: -Xmx1024m
    steps:
      - skip_job_unless_required
      - set_environment_variables
      - checkout
      - run:
          name: Create pull request
          command: |
            target_branch="develop"
            title=${CIRCLE_BRANCH}
            content="${CIRCLE_BRANCH}"
            python3 ./CircleciScripts/create_pullrequest.py "${GITHUB_USER}" "${GITHUB_TOKEN}" "$title" "$content" "$target_branch" "${CIRCLE_PROJECT_USERNAME}:${CIRCLE_BRANCH}" ${CIRCLE_PROJECT_USERNAME} ${CIRCLE_PROJECT_REPONAME}

workflows:
  version: 2

  bump_sdk_version:
    jobs:
      - bump_sdk_version:
          filters:
            branches:
              only:
                - bump_sdk_version
  build_and_test:
    jobs:
      - build:
          filters:
            branches:
              ignore: bump_sdk_version
      - unittest:
          requires:
            - build
          filters:
            branches:
              ignore: bump_sdk_version
      - create_pullrequest_for_modelupdate:
          filters:
            branches:
              only:
                - /^models-update-.+$/
      - pre_integrationtest:
          filters:
            branches:
              only:
                - main
                - develop

      - integrationtest:
          name: AutoScaling
          groupname: AutoScaling
          requires:
            - pre_integrationtest
          filters:
            branches:
              only:
                - main
                - develop

      - integrationtest:
          name: AWSMobileClient
          groupname: AWSMobileClient
          requires:
            - pre_integrationtest
          filters:
            branches:
              only:
                - main
                - develop

      - integrationtest:
          name: CloudWatch
          groupname: CloudWatch
          requires:
            - pre_integrationtest
          filters:
            branches:
              only:
                - main
                - develop

      - integrationtest:
          name: Cognito
          groupname: Cognito
          requires:
            - pre_integrationtest
          filters:
            branches:
              only:
                - main
                - develop

      - integrationtest:
          name: Comprehend
          groupname: Comprehend
          requires:
            - pre_integrationtest
          filters:
            branches:
              only:
                - main
                - develop

      - integrationtest:
          name: Core
          groupname: Core
          requires:
            - pre_integrationtest
          filters:
            branches:
              only:
                - main
                - develop

      - integrationtest:
          name: DynamoDB
          groupname: DynamoDB
          requires:
            - pre_integrationtest
          filters:
            branches:
              only:
                - main
                - develop

      - integrationtest:
          name: EC2
          groupname: EC2
          requires:
            - pre_integrationtest
          filters:
            branches:
              only:
                - main
                - develop

      - integrationtest:
          name: ElasticLoadBalancing
          groupname: ElasticLoadBalancing
          requires:
            - pre_integrationtest
          filters:
            branches:
              only:
                - main
                - develop

      - integrationtest:
          name: IoT
          groupname: IoT
          requires:
            - pre_integrationtest
          filters:
            branches:
              only:
                - main
                - develop

      - integrationtest:
          name: Kinesis
          groupname: Kinesis
          requires:
            - pre_integrationtest
          filters:
            branches:
              only:
                - main
                - develop

      - integrationtest:
          name: KinesisVideoSignaling
          groupname: KinesisVideoSignaling
          requires:
            - pre_integrationtest
          filters:
            branches:
              only:
                - main
                - develop

      - integrationtest:
          name: KMS
          groupname: KMS
          requires:
            - pre_integrationtest
          filters:
            branches:
              only:
                - main
                - develop

      - integrationtest:
          name: Lambda
          groupname: Lambda
          requires:
            - pre_integrationtest
          filters:
            branches:
              only:
                - main
                - develop

      - integrationtest:
          name: Lex
          groupname: Lex
          requires:
            - pre_integrationtest
          filters:
            branches:
              only:
                - main
                - develop

      - integrationtest:
          name: MobileAnalytics
          groupname: MobileAnalytics
          requires:
            - pre_integrationtest
          filters:
            branches:
              only:
                - main
                - develop

      - integrationtest:
          name: Pinpoint
          groupname: Pinpoint
          requires:
            - pre_integrationtest
          filters:
            branches:
              only:
                - main
                - develop

      - integrationtest:
          name: Polly
          groupname: Polly
          requires:
            - pre_integrationtest
          filters:
            branches:
              only:
                - main
                - develop

      - integrationtest:
          name: Rekognition
          groupname: Rekognition
          requires:
            - pre_integrationtest
          filters:
            branches:
              only:
                - main
                - develop

      - integrationtest:
          name: S3
          groupname: S3
          requires:
            - pre_integrationtest
          filters:
            branches:
              only:
                - main
                - develop

      - integrationtest:
          name: SES
          groupname: SES
          requires:
            - pre_integrationtest
          filters:
            branches:
              only:
                - main
                - develop

      - integrationtest:
          name: SNS
          groupname: SNS
          requires:
            - pre_integrationtest
          filters:
            branches:
              only:
                - main
                - develop

      - integrationtest:
          name: SQS
          groupname: SQS
          requires:
            - pre_integrationtest
          filters:
            branches:
              only:
                - main
                - develop

      - integrationtest:
          name: Transcribe
          groupname: Transcribe
          requires:
            - pre_integrationtest
          filters:
            branches:
              only:
                - main
                - develop

      - integrationtest:
          name: TranscribeStreaming
          groupname: TranscribeStreaming
          requires:
            - pre_integrationtest
          filters:
            branches:
              only:
                - main
                - develop

      - integrationtest:
          name: Translate
          groupname: Translate
          requires:
            - pre_integrationtest
          filters:
            branches:
              only:
                - main
                - develop

      - integrationtest:
          name: Textract
          groupname: Textract
          requires:
            - pre_integrationtest
          filters:
            branches:
              only:
                - main
                - develop

      - post_integrationtest:
          requires:
            - AWSMobileClient
            - AutoScaling
            - CloudWatch
            - Cognito
            - Comprehend
            - Core
            - DynamoDB
            - EC2
            - ElasticLoadBalancing
            - IoT
            - KMS
            - Kinesis
            - KinesisVideoSignaling
            - Lambda
            - Lex
            - MobileAnalytics
            - Pinpoint
            - Polly
            - Rekognition
            - S3
            - SES
            - SNS
            - SQS
            - Textract
            - Transcribe
            - TranscribeStreaming
            - Translate
          filters:
            branches:
              only:
                - main
                - develop

      - merge_to_main:
          requires:
            - build
            - unittest
            - post_integrationtest
          filters:
            branches:
              only:
                - develop
      - prepare_release_sdk:
          requires:
            - build
            - unittest
            - post_integrationtest
          filters:
            branches:
              only:
                - main

  release_sdk:
    jobs:
      - build:
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - unittest:
          requires:
            - build
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - pre_integrationtest:
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - integrationtest:
          name: AWSMobileClient
          groupname: AWSMobileClient
          requires:
            - pre_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - integrationtest:
          name: SQS
          groupname: SQS
          requires:
            - pre_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - integrationtest:
          name: MobileAnalytics
          groupname: MobileAnalytics
          requires:
            - pre_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - integrationtest:
          name: Rekognition
          groupname: Rekognition
          requires:
            - pre_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - integrationtest:
          name: SES
          groupname: SES
          requires:
            - pre_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - integrationtest:
          name: SNS
          groupname: SNS
          requires:
            - pre_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - integrationtest:
          name: CloudWatch
          groupname: CloudWatch
          requires:
            - pre_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - integrationtest:
          name: AutoScaling
          groupname: AutoScaling
          requires:
            - pre_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - integrationtest:
          name: Translate
          groupname: Translate
          requires:
            - pre_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - integrationtest:
          name: Transcribe
          groupname: Transcribe
          requires:
            - pre_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - integrationtest:
          name: TranscribeStreaming
          groupname: TranscribeStreaming
          requires:
            - pre_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - integrationtest:
          name: Polly
          groupname: Polly
          requires:
            - pre_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - integrationtest:
          name: Cognito
          groupname: Cognito
          requires:
            - pre_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - integrationtest:
          name: Comprehend
          groupname: Comprehend
          requires:
            - pre_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - integrationtest:
          name: Core
          groupname: Core
          requires:
            - pre_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - integrationtest:
          name: Kinesis
          groupname: Kinesis
          requires:
            - pre_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - integrationtest:
          name: KinesisVideoSignaling
          groupname: KinesisVideoSignaling
          requires:
            - pre_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - integrationtest:
          name: KMS
          groupname: KMS
          requires:
            - pre_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - integrationtest:
          name: ElasticLoadBalancing
          groupname: ElasticLoadBalancing
          requires:
            - pre_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - integrationtest:
          name: EC2
          groupname: EC2
          requires:
            - pre_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - integrationtest:
          name: Lambda
          groupname: Lambda
          requires:
            - pre_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - integrationtest:
          name: Lex
          groupname: Lex
          requires:
            - pre_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - integrationtest:
          name: IoT
          groupname: IoT
          requires:
            - pre_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - integrationtest:
          name: Pinpoint
          groupname: Pinpoint
          requires:
            - pre_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - integrationtest:
          name: S3
          groupname: S3
          requires:
            - pre_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - integrationtest:
          name: DynamoDB
          groupname: DynamoDB
          requires:
            - pre_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - integrationtest:
          name: Textract
          groupname: Textract
          requires:
            - pre_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - post_integrationtest:
          requires:
            - AWSMobileClient
            - AutoScaling
            - CloudWatch
            - Cognito
            - Comprehend
            - Core
            - DynamoDB
            - EC2
            - ElasticLoadBalancing
            - IoT
            - KMS
            - Kinesis
            - KinesisVideoSignaling
            - Lambda
            - Lex
            - MobileAnalytics
            - Pinpoint
            - Polly
            - Rekognition
            - S3
            - SES
            - SNS
            - SQS
            - Textract
            - Transcribe
            - TranscribeStreaming
            - Translate
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      # TODO: REMOVE THIS BEFORE MERGING
      - build_carthage_palpatim_test:
          filters:
            branches:
              only: palpatim/xcode-12-build-fix
      - lint_cocoapods_palpatim_test:
          filters:
            branches:
              only: palpatim/xcode-12-build-fix
      # TODO: END REMOVE
      - build_carthage:
          requires:
            - unittest
            - post_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - release_ios_s3:
          requires:
            - build_carthage
            - unittest
            - post_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - release_tag:
          requires:
            - unittest
            - post_integrationtest
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - release_docs:
          requires:
            - release_tag
            - release_ios_s3
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - add_doc_tag:
          requires:
            - release_docs
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - bump_ios_amplifydocs_version:
          requires:
            - release_tag
            - release_ios_s3
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - release_cocoapods:
          requires:
            - add_doc_tag
            - bump_ios_amplifydocs_version
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - release_carthage:
          requires:
            - build_carthage
            - add_doc_tag
            - bump_ios_amplifydocs_version
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
      - bump_ios_sampleapp_version:
          requires:
            - release_tag
            - release_ios_s3
            - release_cocoapods
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+.[0-9]+.[0-9]+$/
